[project]
name = "vcc-analysis-go"
version = "0.1.0"
description = "VCC Platform — Go Analysis Service (KataGo)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
]
