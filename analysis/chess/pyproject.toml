[project]
name = "vcc-analysis-chess"
version = "0.1.0"
description = "VCC Platform — Chess Analysis Service (Stockfish)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "python-chess>=1.999",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
]
